# Co-Create Company Explorer 詳細設計書

本書は、学生団体向け協賛企業探索プラットフォーム「Co-Create Company Explorer」のプロジェクト構成、UI/UX仕様、ドメインモデル、技術スタック、運用方針をクリーンアーキテクチャに基づき詳細化した設計ドキュメントである。

---

## 1. プロジェクト概要

### 1.1 基本情報
- **プロジェクト名**：Co-Create Company Explorer  
- **主要ユーザー**：協賛・共創パートナーを探す学生団体  
- **想定ターゲットページ**：  
  - `企業を探す`：`/browse/company`  
  - `企業詳細モーダル`：`/company/{id}`（モーダルまたは別ページ遷移）  
- **UIコンセプト**：白基調にアクセントカラー `#003366` を組み合わせ、信頼性・清潔感・スピード感を表現  
- **参考UI**：matcher.jp のカードUIをベースに学生団体向けへ最適化  

### 1.2 プロジェクト目的
- 協賛・共創が可能な企業情報を一目で比較できるカード型グリッド体験を提供する  
- 条件絞り込みと詳細情報閲覧、提携申込までをシームレスに繋ぎ、学生団体の意思決定を支援する  
- 将来的な相性スコアリングやレコメンド拡張を見据えた柔軟なアーキテクチャ設計を行う  

---

## 2. クリーンアーキテクチャ全体像

### 2.1 レイヤ構成と依存方向

| レイヤ | 主な責務 | 依存方向 |
| --- | --- | --- |
| **Presentation Layer** | Next.js/React によるUI実装、ページ遷移、レスポンシブ対応、アクセシビリティ管理 | Interface Layer |
| **Interface Layer** | API/BFF コントローラ、DTOマッパー、認証・認可、セッション管理 | Application Layer |
| **Application Layer** | ユースケース調停、サービスコーディネーション、リポジトリインターフェース定義、入力/出力バリデーション | Domain Layer |
| **Domain Layer** | エンティティ、値オブジェクト、ドメインサービス、ビジネスルール | 外部依存なし |
| **Infrastructure Layer** | ORM/DB アクセス、キャッシュ、外部API連携、通知サービス実装、ファイルストレージ | Interface/Application Layer |

**原則**：内側レイヤは外側に依存しない。プレゼンテーションで使用するデータは必ずユースケースを経由し、ドメイン知識は Domain Layer に集約する。

### 2.2 技術スタック（推奨例）
- **フロントエンド**：Next.js (App Router) / TypeScript / React Query or TanStack Query / Zustand / Tailwind CSS または Emotion  
- **バックエンド**：Node.js (NestJS or Express) / TypeScript / PostgreSQL / Prisma or TypeORM / Redis  
- **外部連携**：メール or Slack 通知、ElasticSearch or Algolia（高度検索時）  
- **CI/CD**：GitHub Actions（lint → test → build → no-emit build → deploy）  
- **テスト**：Jest（ユニット）、Testing Library（コンポーネント）、Playwright or Cypress（E2E）、Storybook + Visual Regression  
- **監視**：OpenTelemetry / Datadog / Sentry（エラートラッキング）

---

## 3. UI/UX 詳細仕様

### 3.1 ビジュアル言語
- **基調色**：`#ffffff`  
- **アクセント**：`#003366`（Primary）、hover時 `#00294f`  
- **補助色**：`#E6ECF3`（タグ・背景）  
- **文字色**：メイン `#333333`、サブ `#666666`  
- **フォント**：`Inter` / `Noto Sans JP`（和英混在時も調和）  
- **角丸**：12〜16px を統一（ボタン、カード、タブ）  
- **影**：影は通常 `shadow-sm`、hover時 `shadow-md`  
- **スペーシング**：カード内余白 24px、グリッド gap 20px、セクション間 48px  

### 3.2 ページ構成 - PC レイアウト

1. **ヘッダー**
   - 左上：ブランドロゴ「Co-Create」  
   - 右上ナビゲーション：`案件一覧` / `企業一覧`（強調表示）/ `ダッシュボード` / `ログイン状態（アイコン＋氏名）`  
   - 背景：白、下部に `#E6ECF3` の1px ボーダー、固定高さ 72px  
   - レスポンシブ：SP時はハンバーガーメニュー + ユーザーアイコン  

2. **ファーストビュー**
   - タイトル：`協賛・共創できる企業を探す`（H1、24px）  
   - 検索バー：キーワード入力（プレースホルダ例「企業名・キーワードで検索」）  
   - フィルタタブ：  
     - **業界**：人材 / IT / メーカー / 教育 / ...  
     - **協賛タイプ**：金銭協賛 / 物品提供 / メンタリング / イベント共催  
     - **地域**：東京 / 関西 / オンライン  
     - 選択状態は `#003366`、非選択は `#E6ECF3` 背景  
     - SP版は横スクロール可能なタブ表示  
   - 並び替えドロップダウン：`評価が高い` / `新着順` / `募集数が多い`  

3. **企業カードグリッド**
   - `display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 20px;`  
   - hover：`transform: translateY(-4px)`、shadow強化、CTAカラー濃化  
   - SP/タブレット：2列 → 1列へスイッチ  

4. **フッター（簡易）**
   - コピーライト、問い合わせリンク、プライバシーポリシー等  
   - 背景 `#E6ECF3`、テキスト `#666666`

### 3.3 企業カード構成（matcher型を踏襲）

| セクション | 内容 | 詳細仕様 |
| --- | --- | --- |
| 上部タグ帯 | 業界・大学連携・特徴など最大3つ | 背景 `#E6ECF3` or brand-50、文字 `#003366`、角丸 12px、タグ間 8px |
| 企業情報 | ロゴ（丸形 64px）、会社名、担当者名、役職、評価 | 会社名は16px太字、担当者情報は`#666666`、星評価は `平均値 + 件数` 表示 |
| 本文エリア | 協賛プランタイトル、1行サマリー、背景画像 | タイトルは太字18px、サマリー14px、背景は 8% ブラーのブランドカラー or グレーイメージ |
| CTA ボタン | `もっと詳しく` | 角丸16px、背景 `#003366`、文字白、hoverで `#00294f`、transition 0.25s |
| 下部リンク | `すべての協賛プランを見る` | テキストカラー `#003366`、hoverで下線表示、12px |

### 3.4 詳細モーダル / 別ページ構成

1. **ヘッダー**
   - 左に企業ロゴと企業名（24px）、右に星評価 + レビュー数  
   - 代表者コメント or 企業理念（最大120文字、`#333333`）  

2. **協賛条件ブロック**
   - 各条件をチェックアイコン付きリスト表示  
   - 例：`💰 金銭協賛あり（上限30万円まで）` / `📦 物品提供（飲料・印刷物）` / `💬 メンタリング可（週1回打合せ）` / `🤝 共催イベント（大学内イベント支援）`  
   - true/false 状態に応じて強調 or グレーアウト  

3. **実績紹介カード**
   - 過去協賛した団体・イベント名、ロゴ、成果サマリー  
   - CTA：`成功事例を見る`（セカンダリボタン）  
   - カードレイアウト：背景 `#ffffff`、角丸16px、shadow-sm  

4. **応募/提案フォーム**
   - 団体情報：組織名、代表者、連絡先（ログイン情報から自動入力、編集可否要検討）  
   - 提案メッセージ：最大1000文字テキストエリア  
   - CTA：`提携を申し込む`（Primaryボタン、幅100%、角丸16px）  
   - 入力バリデーション：必須項目チェック、文字数制限、送信中インジケータ  

5. **送信完了モーダル**
   - 内容：`送信完了` / `企業に通知しました`  
   - ボタン：`OK`（閉じる）  
   - 補足：企業ダッシュボードで通知される旨を説明  

### 3.5 レスポンシブ / モーション
- SP：カード1列、CTA下部固定、フィルタ横スクロール、モーダルは全画面スライド  
- トランジション：`transition: all 0.25s ease-in-out;`  
- ホバー：影強調、CTA濃色、下部リンク下線  
- アニメーション：モーダルスライドイン、フィルタタブ切替時のフェード  
- アクセシビリティ：キーボード操作、フォーカストラップ、ARIA 属性付与、コントラスト比 WCAG 2.1 AA 以上

---

## 4. 体験フロー（学生団体視点）

1. `/browse/company` を開く  
2. キーワード検索・フィルタ（業界/協賛タイプ/地域）で条件絞り込み  
3. カードで企業情報・協賛条件・実績概要を確認  
4. 気になる企業カードの `もっと詳しく` をクリック → 詳細モーダルが開く  
5. 協賛条件と成功事例を詳細確認し、提案メッセージを作成  
6. `提携を申し込む` ボタンで送信 → 完了モーダル表示  
7. 企業ダッシュボードへ通知（擬似体験でも応答を可視化）  

---

## 5. ドメインモデル

### 5.1 エンティティ / 値オブジェクト

| モデル | 主なフィールド | 説明 |
| --- | --- | --- |
| **Company** (Entity) | `id`, `name`, `logoUrl`, `industryTags`, `rating`, `reviewCount`, `primaryContact` | 企業基礎情報 |
| **PrimaryContact** (Value Object) | `name`, `role`, `email`, `phone` | 担当者情報（不変条件：email形式、電話番号フォーマット） |
| **SponsorshipPlan** (Entity) | `id`, `companyId`, `planTitle`, `summary`, `sponsorshipTypes`, `coverageArea`, `image` | 協賛プラン情報 |
| **SponsorshipCondition** (Value Object) | `cashSupport`, `goodsSupport`, `mentoring`, `cohostEvent`, 各詳細 | 協賛条件、真偽と上限値など |
| **Achievement** (Entity) | `id`, `companyId`, `organizationName`, `eventName`, `description`, `logoUrl` | 過去協賛実績 |
| **Proposal** (Entity) | `id`, `organizationId`, `companyId`, `message`, `status`, `submittedAt` | 提携申込データ |
| **Tag** (Value Object) | `type`, `label` | タグ区分：業界/特徴/大学連携など |
| **Rating** (Value Object) | `score`, `count` | 口コミ評価（score 0-5, count >=0） |

### 5.2 ドメインサービス（例）
- `CompanyFilterService`：業界・協賛タイプ・地域・評価などの複合フィルタロジック  
- `ProposalSubmissionPolicy`：同一企業への重複申込、レート制限、団体プロフィール要件の検証  
- `RecommendationService`（将来拡張）：相性スコア算出、レコメンド生成  

---

## 6. アプリケーションレイヤ（ユースケース）

| ユースケース | 入力 | 出力 | 依存するリポジトリ / サービス |
| --- | --- | --- | --- |
| `SearchCompaniesUseCase` | キーワード、業界、協賛タイプ、地域、並び替え、ページング | `CompanyListViewModel` | `CompanyReadRepository`, `CompanyFilterService`, キャッシュ |
| `ViewCompanyDetailUseCase` | `companyId` | 企業詳細（プラン、実績、条件） | `CompanyReadRepository`, `SponsorshipPlanRepository`, `AchievementRepository` |
| `SubmitProposalUseCase` | `organizationId`, `companyId`, `message` | 結果ステータス | `ProposalRepository`, `NotificationService`, `ProposalSubmissionPolicy` |
| `LoadFiltersUseCase` | なし | フィルタ選択肢マスタ | `TagRepository`, `RegionRepository` |
| `LoadRecommendedCompaniesUseCase`（拡張） | `organizationId` | 推薦企業リスト | `RecommendationService`, `CompanyReadRepository` |

各ユースケースは入力 DTO → バリデーション → ドメインロジック呼び出し → 出力 DTO 変換を行い、副作用（通知・ログなど）は Application 層で完結させる。

---

## 7. インターフェース / インフラ設計

### 7.1 API エンドポイント（例）
- `GET /api/companies`：一覧取得（クエリ：`keyword`, `industries[]`, `sponsorshipTypes[]`, `regions[]`, `sort`, `page`, `limit`）  
- `GET /api/companies/{id}`：企業詳細（条件、プラン、実績）  
- `POST /api/companies/{id}/proposals`：提携申込（ボディ：`organizationId`, `message`）  
- `GET /api/filters`：フィルタ候補（業界、地域、協賛タイプ）  
- `GET /api/recommendations`（拡張）：学生団体用推薦企業  

### 7.2 データベース設計（PostgreSQL 例）

| テーブル | 主なカラム | 備考 |
| --- | --- | --- |
| `companies` | `id`, `name`, `logo_url`, `rating_score`, `rating_count`, `primary_contact_id` | 企業基本情報 |
| `company_contacts` | `id`, `company_id`, `name`, `role`, `email`, `phone` | 担当者情報 |
| `company_tags` | `company_id`, `tag_id` | 多対多中間テーブル |
| `tags` | `id`, `type`, `label` | タグマスタ |
| `sponsorship_plans` | `id`, `company_id`, `title`, `summary`, `types`, `coverage_area`, `image_url` | 協賛プラン |
| `sponsorship_conditions` | `company_id`, `cash_support`, `goods_support`, `mentoring`, `cohost_event`, `details_json` | 条件フラグと詳細 |
| `achievements` | `id`, `company_id`, `organization_name`, `event_name`, `description`, `logo_url` | 実績紹介 |
| `proposals` | `id`, `company_id`, `organization_id`, `message`, `status`, `submitted_at` | 提携申込 |
| `organizations` | `id`, `name`, `representative`, `email`, `phone`, `profile_json` | 学生団体情報 |

### 7.3 キャッシュ / 外部連携
- 検索レスポンスキャッシュ：Redis（キー：条件ハッシュ、TTL 5分）  
- 通知サービス：メール（SendGrid など）や Slack Webhook による企業通知  
- 画像ホスティング：S3 + CDN（CloudFront）  
- ログ基盤：構造化ログを S3/Elasticsearch へ転送

---

## 8. フロントエンド実装詳細

### 8.1 ページ / ルーティング
- `/browse/company`：企業カードグリッド、検索バー、フィルタタブ、並び替え  
- `/browse/company/[id]`：モーダル or 新ページ、SSR/SSG 検討  
- `/dashboard`：企業側通知確認（将来拡張）  

### 8.2 コンポーネント構造（例：Atomic Design 準拠）
- **Atoms**：`Button`, `TagChip`, `Avatar`, `RatingStars`, `Icon`, `Input`, `Textarea`, `Dropdown`, `Badge`  
- **Molecules**：`SearchBar`, `FilterTabs`, `CompanyInfo`, `ConditionChecklist`, `AchievementCard`  
- **Organisms**：`CompanyCard`, `CompanyGrid`, `CompanyDetailModal`, `ProposalForm`, `Header`, `Footer`  
- **Templates**：`BrowseCompanyTemplate`, `CompanyDetailTemplate`  
- **Pages**：`BrowseCompanyPage`, `CompanyDetailPage`  

### 8.3 ステート管理
- サーバーステート：React Query（検索条件 → キャッシュキーで管理）  
- UIステート：Zustand or Context（モーダル開閉、選択フィルタ、並び替え）  
- フィルタ状態は URL クエリと同期し、シェア可能にする  

### 8.4 アクセシビリティ / 国際化
- モーダルのフォーカストラップ、`aria-modal`, `role="dialog"` 適用  
- キーボード操作（Tab/Shift+Tab、Enter、Escape）  
- 文言は i18n ライブラリで管理し、将来の多言語対応を容易化  

---

## 9. バックエンド実装詳細

### 9.1 リポジトリ / アダプタ
- `CompanyReadRepository`：企業一覧・詳細取得（フィルタ＆並び替え対応）  
- `SponsorshipPlanRepository`：プラン情報取得  
- `AchievementRepository`：実績情報取得  
- `ProposalRepository`：提携申込作成・状態管理  
- `TagRepository`, `RegionRepository`：フィルタマスタ  
- `NotificationService`：申込通知（メール/SNS）  

### 9.2 バリデーション / 認証
- リクエスト DTO でスキーマバリデーション（Zod or class-validator）  
- 認証：JWT or セッション（学生団体ログイン状態をヘッダーで表示）  
- 権限管理：学生団体ロールのみに提携申込権限を付与  
- Rate Limiting：申込 API へ IP/ユーザー単位制限  

### 9.3 ログ / モニタリング
- 申込時の監査ログ（誰がいつどの企業へ）  
- エラーログは Sentry へ送信  
- メトリクス：検索APIのレスポンス時間、申込数推移、モーダル開封率  

---

## 10. 非機能要件

| カテゴリ | 要件 |
| --- | --- |
| パフォーマンス | 検索API応答 < 300ms（キャッシュ活用）、LCP 2.5s 以下 |
| セキュリティ | CSRF/XSS 対策、HTTPS/TLS、入力サニタイズ、Rate Limiting |
| 可用性 | 稼働率 99%以上、デプロイはブルー/グリーン、ロールバック手順整備 |
| スケーラビリティ | 水平方向スケール（APIサーバ）、CDN で静的資産配信 |
| テスト | ユニット70%カバレッジ、主要フローE2E自動化、アクセシビリティ監査 |
| 運用 | ログ収集、アラート閾値設定、ドキュメント整備（手順書/ERD/API Spec） |

---

## 11. テスト戦略

### 11.1 テストピラミッド
- **ユニットテスト**：ドメインモデル、ユースケース、フィルタロジック  
- **コンポーネントテスト**：カードUI、モーダル、フォームバリデーション  
- **統合テスト**：API経由でユースケースが正しく実行されるか  
- **E2Eテスト**：学生団体のシナリオ（検索 → 詳細閲覧 → 申込）を自動化  
- **視覚回帰テスト**：Storybook で主要コンポーネントのデザイン崩れ検知  

### 11.2 QA プロセス
1. デザインレビュー（UI/UX観点の確認）  
2. コードレビュー（クリーンアーキテクチャ準拠）  
3. 自動テスト実行（CI）  
4. ステージング環境での操作確認（アクセシビリティ含む）  

---

## 12. 運用・拡張計画

### 12.1 将来的な拡張
- 企業ごとの協賛履歴タイムライン表示  
- 学生団体との相性スコアリング（満足度、応募成功率、活動ジャンル）  
- レコメンド欄：`あなたの団体にマッチする企業` 表示、推薦理由も提示  
- ダッシュボードでの KPI 可視化（申込数、マッチング率、イベント支援実績）  
- 企業評価レビュー投稿機能（テキスト + ⭐️評価）  

### 12.2 データ活用
- 検索条件・クリックログを蓄積し、人気の条件や改善領域を分析  
- スコアリングモデルの学習データとして、申込結果と満足度を収集  
- フィードバックサイクル：学生団体と企業からの声を収集しUI改善へ反映  

### 12.3 運用体制
- タスク管理：Issue テンプレート（要件 / UI / テクニカル）  
- スプリント：2週間サイクル（Plan → Build → Review → Retro）  
- ドキュメント：Notion or Confluence、Storybook、OpenAPI Spec  
- コミュニケーション：Slack / Teams、週次進捗 MTG、デザインデイリー  

---

## 13. リスクと対応策

| リスク | 想定影響 | 対応策 |
| --- | --- | --- |
| 企業情報更新の遅延 | 情報の鮮度低下、学生団体の信用低下 | 企業側ダッシュボード整備、更新リマインダ通知 |
| スパム申込 | 企業側の負荷増大 | CAPTCHA、Rate Limiting、申込前審査（団体プロフィール充足チェック） |
| デザイン崩れ（レスポンシブ） | UX悪化 | Storybook + Visual Regression、レスポンシブチェックリスト |
| データ不整合 | 申込進行不能 | トランザクション管理、DB整合性制約、監査ログ |

---

## 14. まとめ

Co-Create Company Explorer は、学生団体が協賛・共創企業を迅速に見つけられることを最優先に設計されたプラットフォームである。本設計書では、クリーンアーキテクチャに沿ったレイヤ分割、UI/UX仕様、ドメインモデル、ユースケース、非機能要件、運用計画を詳細に整理した。  
この構成を基盤とし、学生団体・企業双方にとって信頼性が高く拡張可能なマッチング体験を実現する。

---

© 2025 Co-Create Project Team. All rights reserved.

